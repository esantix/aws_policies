name: Testing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
 test:
   uses: esantix/workflows/.github/workflows/python_unittest.yml@main
   with:
     requirements: -r requirements.dev.txt -e .
